<?xml version="1.0" encoding="utf-8"?>
<odoo>

     <record id="point_of_sale.pos_invoice_report" model="ir.actions.report">
        <field name="name">Invoice</field>
        <field name="model">pos.order</field>
        <field name="report_type">qweb-pdf</field>
        <field name="report_name">pos_invoice_report_custom.report_invoice_custom</field>
        <field name="print_report_name">'Invoice - %s' % (object.name)</field>
    </record>

    <template id="report_invoice_custom" inherit_id="pos_invoice_report_custom.report_pos_invoice_custom" primary="True"/>

</odoo>
